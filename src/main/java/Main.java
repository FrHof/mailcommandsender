/*import javax.mail.Session;
import com.emailsender.EmailSender;*/
public class Main {
	public static void main(String[] args) {
		// INIT VALUES FOR CONNECTING TO GMAIL
		/*final String from = "************";
		final String passw = "************";
		final String to = "************";
		final String host = "smtp.gmail.com";
		final String port = "587";
		// INIT VALUE FOR THE CONTENT OF THE MESSAGE :
		final String subject = "Commande fromage ";
		final String content = "Bonjour, voici la commande pour M.Dupont";
		EmailSender sender = new EmailSender(from,passw,to,host,port);
		Session session = sender.initSession();
		sender.sendEmail(session, subject, content);*/
		
	}
}
